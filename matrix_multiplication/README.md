## CUDA GEMM Experiments

This repository contains ongoing CUDA kernel experiments focused on
matrix multiplication and low-level GPU performance tuning (A100).

The code explores shared-memory tiling, register-level sub-tiling,
and profiling-driven optimization using NVIDIA Nsight Compute.

This is a research / experimentation repo, not a polished library.
